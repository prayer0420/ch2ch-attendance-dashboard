# 런북을 보면서 녹음에서 설명한 내용 정리

## 먼저 결론

이 녹음의 핵심은 다음 한 문장으로 정리된다.

> VM을 복사하거나 미러로 가져오는 것은 출발점일 뿐이고, 서비스를 전부 똑같이 “파일 복사”하는 것이 최종 이관 방법은 아니다. 서비스마다 데이터의 성격이 다르므로, Elasticsearch와 Solr는 안전한 백업·복원 절차를 사용하고, Redis는 필요하면 빈 클러스터를 다시 만든 뒤 캐시를 재생성하며, MariaDB는 쓰기를 멈춘 뒤 필요한 데이터만 정합화한다.

즉, 녹음에서 말한 것은 “새 VM에서 모든 것을 새로 설치하자”는 뜻도 아니고, “VM을 복사했으니 그대로 운영에 연결하자”는 뜻도 아니다.

정확한 흐름은 다음과 같다.

```text
기존 VM 미러/복사
        ↓
운영망과 분리된 target VM 부팅
        ↓
서비스별로 그대로 재결합할지, 새로 구성·복원할지 판단
        ↓
DB·검색엔진·캐시·애플리케이션 연결 검증
        ↓
특정 테스트 IP 또는 격리된 L4 경로로 시험
        ↓
문제 없을 때만 운영 트래픽에 단계적으로 연결
```

---

## 1. 이 문서를 읽을 때 먼저 구분할 것

### 런북은 “실행 절차”이고, 녹음은 “그 절차의 판단 이유”다

첨부된 HTML 런북은 `VMware 미러 기반 Oracle Linux 9.6 전환 작업지침서`다. 런북에는 사전 백업, target 격리, MariaDB·Redis·ZooKeeper/Solr·Elasticsearch 복원, 애플리케이션 기동, 리허설, 최종 동기화, GO/NO-GO와 롤백 절차가 적혀 있다.

녹음은 런북을 처음부터 끝까지 낭독한 것이 아니다. 녹음에서 들리는 부분은 특히 다음 판단을 설명하는 대화다.

- Elasticsearch는 단순히 data 디렉터리를 복사하지 말고 Snapshot/Restore를 사용한다.
- Solr collection은 반드시 백업·복원해야 한다.
- Redis는 cache 성격이므로 기존 cluster 재결합을 먼저 시도하되, 실패하면 빈 cluster를 새로 만들고 시스템 배포로 cache를 채운다.
- MariaDB는 함부로 동시에 켜거나 전체를 덮어쓰지 않고, 쓰기를 멈춘 상태에서 절차에 맞게 처리한다.
- 서비스가 뜨는 것만 확인하지 말고, 마지막에는 실제 테스트 IP 또는 트래픽 경로를 연결해 전체 흐름을 본다.

### 현재 Xen 이관과의 관계

이 런북은 VMware와 Oracle Linux 9.6을 전제로 작성됐다. 기존에 분석한 환경은 Xen 호스트와 Rocky Linux VM이므로 VMware 전용 명령, Oracle Linux 전용 패키지 설치 가정, VMware 네트워크 용어를 그대로 실행하면 안 된다.

다만 다음 원칙은 Xen 환경에도 그대로 참고할 수 있다.

- VM 복사 후 바로 운영망에 붙이지 않는다.
- source와 target이 같은 IP·hostname으로 동시에 보이지 않게 한다.
- 분산 서비스의 데이터 디렉터리를 무조건 복사하지 않는다.
- 서비스별로 “그대로 재사용”과 “빈 구성 후 복원”을 나눈다.
- Scheduler와 writer를 먼저 멈추고, 데이터가 더 이상 변하지 않는 시점을 만든다.
- 검증이 끝난 뒤 특정 테스트 경로를 거쳐 운영 전환한다.

따라서 이 문서는 Xen 이관에 사용할 명령어 모음이 아니라, **이관 설계를 세울 때 참고하는 원칙과 판단 기준**으로 읽어야 한다.

---

## 2. 녹음에서 설명한 내용을 시간순으로 해석

아래의 “해석”은 자동 전사 문장을 런북의 문맥과 대조한 것이다. 녹음 음질 때문에 명확하게 들리지 않은 단어는 억지로 확정하지 않았다.

### 2.1 약 12:44 — Elasticsearch는 Snapshot/Restore가 가장 안전하다는 설명

자동 전사에는 다음과 같이 잡혔다.

> “이거는 … Elasticsearch … 스냅샷을 뜨고 리스토를 해”
>
> “그냥 뜬다고 해서 되는 게 아닙니다”
>
> “얘는 그게 제일 안전해요”

여기서 말하는 뜻은 다음과 같다.

Elasticsearch는 여러 노드가 하나의 클러스터를 이루고, 각각의 data 디렉터리 안에 shard·translog·node 정보·cluster UUID 같은 내부 상태를 저장한다. 이 디렉터리를 VM 미러에서 그대로 가져와 새 클러스터에 넣으면, 세 노드가 같은 논리 시점의 데이터라는 보장이 없고, 예전 cluster UUID나 노드 정보가 새 환경에 섞일 수 있다.

그래서 런북은 다음처럼 분리한다.

1. 미러의 Elasticsearch data는 즉시 삭제하지 않고 `hold` 보관본으로 둔다.
2. target에는 빈 data 디렉터리로 새로운 Elasticsearch cluster를 만든다.
3. source에서 완료된 Snapshot을 repository를 통해 target으로 Restore한다.
4. index·count·mapping·settings·alias를 비교한다.
5. template·pipeline·ILM·SLM 같은 정책 객체는 Snapshot에 전부 자동으로 안전하게 따라온다고 가정하지 않고 별도 export/import·검증한다.

즉 녹음의 “스냅샷을 뜨고 리스토어”는 단순 파일 복사의 반대 개념이다. 파일을 운반하는 것이 아니라 Elasticsearch가 이해하는 백업 형식으로 데이터와 구조를 복원하는 것이다.

### 2.2 약 13:05 — Solr collection은 빠뜨리면 안 된다는 설명

자동 전사에는 다음과 같이 잡혔다.

> “솔라 컬렉션은 아까 말씀하셨지만 …”
>
> “그러니 그냥 파일 떠서 …”
>
> “얘도 컬렉션 …”

이 부분은 일부 단어가 불명확하지만, 런북의 해당 게이트와 연결하면 의미는 비교적 분명하다. **Solr collection은 반드시 이관 대상이며, collection별 BACKUP/RESTORE 완료 여부를 확인해야 한다**는 설명이다.

Solr에서 collection은 단순한 폴더 이름이 아니다. collection 안에는 shard, replica, leader, configset, alias, ZooKeeper에 저장되는 클러스터 설정이 함께 얽혀 있다. 따라서 Solr data 폴더와 ZooKeeper transaction log를 그대로 복사하는 것으로 끝내면 안 된다.

런북의 의미는 다음과 같다.

- source의 모든 collection 이름을 먼저 확인한다.
- collection별로 hard commit을 해서 검색 가능한 문서를 안정화한다.
- Solr의 BACKUP을 실행하고 비동기 요청이 `completed`인지 확인한다.
- target의 새 ZooKeeper ensemble과 Solr cloud를 만든다.
- configset을 올리고 collection을 RESTORE한다.
- shard·replica·leader가 모두 정상인지, collection별 문서 수와 대표 검색 결과가 맞는지 확인한다.
- alias와 한국어 분석기·플러그인·JAR checksum도 확인한다.

따라서 녹음에서 Solr를 말한 부분은 “Solr도 VM에 들어 있으니 디렉터리만 가져가면 된다”는 뜻으로 보면 안 된다. **collection이라는 논리 단위로 백업하고 복원해야 한다**는 의미로 보는 것이 맞다.

### 2.3 약 13:27 — Redis는 데이터가 없어도 되고, 실패하면 cluster를 새로 만들 수 있다는 설명

자동 전사에는 비교적 명확하게 다음 내용이 잡혔다.

> “레디스는 데이터가 없어도 돼”
>
> “잘 되면 상관없고 안 되면 데이터는 다 비우고 클러스터를 새로 만들어줘야 합니다”
>
> “그 내용이 적혀있는 거예요”

이 말은 Redis의 모든 값을 무시해도 된다는 뜻이 아니다. 현재 런북의 Redis는 **재생성 가능한 cache**라는 전제를 둔다는 뜻이다.

Redis 처리 순서는 두 갈래다.

#### 우선 시도: 기존 cluster 재결합

VM 미러에 들어 있는 Redis의 정적 설정, 자동 cluster state, AOF/RDB가 서로 맞고 node ID·slot·master/replica 관계도 정상이라면, source를 격리한 뒤 target에서 기존 cluster를 재결합한다.

합격 기준의 예시는 다음과 같다.

- cluster state가 `ok`
- 16,384개 slot이 모두 할당·정상
- master 3개, replica 3개
- known node 6개
- fail·handshake·noaddr·migrating·importing 문제가 없음

#### 대체 경로: 빈 cluster 재구성

기존 cluster state가 꼬였거나 AOF/RDB와 node 상태가 맞지 않으면, 억지로 node ID나 slot을 수선하지 않는다.

1. 여섯 Redis 프로세스를 중지한다.
2. 자동 cluster state·AOF·RDB를 hold 영역에 보관한다.
3. 빈 working directory에서 6개 인스턴스를 새로 시작한다.
4. 7000 세 개를 master로 구성한다.
5. 7001 세 개를 서로 다른 물리 VM의 replica가 되도록 구성한다.
6. cluster가 정상인 것을 확인한다.
7. 애플리케이션의 시스템 배포 기능으로 cache를 다시 채운다.

그러므로 “Redis 데이터가 없어도 된다”는 표현은 **Redis가 저장하는 값이 cache라면 원본 데이터베이스·Solr·Elasticsearch를 기준으로 재생성할 수 있다**는 뜻이다. Redis를 무조건 지우라는 지시가 아니다. 기존 cluster가 정상인지 먼저 확인하고, 실패할 때만 빈 cluster로 전환한다.

### 2.4 약 13:45 이후 — 나머지 서비스와 MariaDB를 구분해서 처리한다는 설명

자동 전사에는 다음과 같은 표현이 있다.

> “나머지는 그냥 옮겨가면 되는 거고요”
>
> “실행하면 돼야 되고”
>
> “MariaDB 같은 경우는 마찬가지예요”
>
> “그냥 꺼서 돼야 돼요”

이 부분은 음질 때문에 정확한 문장을 확정하기 어렵다. 다만 앞뒤 런북 내용과 연결하면, “나머지는 무조건 파일 복사로 끝난다”는 뜻으로 해석하면 위험하다.

런북은 서비스를 다음처럼 다룬다.

- 실행 파일·설정·로그·업로드 파일은 checksum과 권한을 확인해 보존한다.
- 분산 데이터 서비스인 Elasticsearch, Solr/ZooKeeper, Redis는 각자의 복원 절차를 사용한다.
- MariaDB는 미러 DB를 기준으로 삼되, Scheduler와 애플리케이션 writer를 멈춘 상태에서 필요한 데이터만 재적재·교정한다.
- 애플리케이션은 DB·Redis·Solr·Elasticsearch의 target 주소를 바라보는지 확인한 뒤 단계적으로 올린다.

따라서 MariaDB에 대한 녹음의 핵심은 “MariaDB를 source와 target에서 동시에 쓰게 두지 말고 먼저 꺼 두어야 한다”는 뜻으로 이해하는 것이 안전하다.

### 2.5 약 15:34 전후 — 리허설에서 실제 시스템 배포를 다시 해볼 수 있다는 설명

자동 전사에는 다음과 같이 잡혔다.

> “리스토리 일단 그냥 돌려보기만”
>
> “잘 되어 있으면 그냥 스킵”
>
> “시스템 배포도 다시 해주면 되요”

여기서 중요한 개념은 **리허설에서 한 번 시험하고, 실패했을 때 대체 경로를 재현할 수 있어야 한다**는 것이다.

예를 들어 Redis를 새 빈 cluster로 만들었다면 실제 시스템 배포 기능을 통해 cache가 다시 생성되는지 시험해야 한다. 화면이 뜨는지만 보는 것이 아니라 다음을 확인해야 한다.

- 배포가 실제로 성공했는가
- Redis에 필요한 key가 생겼는가
- key의 type·value·TTL이 정상인가
- 애플리케이션이 새 Redis를 통해 정상 조회하는가
- 실패한 배포나 중복 배포가 남지 않았는가

같은 원리는 Elasticsearch Snapshot Restore, Solr collection Restore, MariaDB 선택 재적재에도 적용된다. “명령이 실행됐다”가 아니라 “복원 후 서비스가 실제 업무를 수행했다”가 합격 기준이다.

### 2.6 약 16:37 이후 — Elasticsearch/Kibana 절차를 런북에서 확인하라는 부분

자동 전사 중 일부는 다음처럼 인식됐다.

> “그 다음 Elasticsearch, Kibana”
>
> “어떤 절차로 어떻게 하라고 나와있어요?”

이 부분은 런북의 11장과 연결된다. Elasticsearch를 올린 뒤 바로 Kibana만 띄우는 것이 아니라, 다음 순서를 확인해야 한다.

1. 빈 Elasticsearch cluster가 만들어졌는가
2. Snapshot repository가 target에서 verify 되었는가
3. application index와 Kibana physical index를 올바르게 Restore했는가
4. global state를 무조건 자동 복원하지 않았는가
5. template·pipeline·ILM·SLM을 별도로 적용했는가
6. shard·alias·mapping·count·검색 결과가 맞는가
7. ILM/SLM을 재개해도 즉시 rollover·delete·allocation이 발생하지 않는가
8. 그 후에 Kibana를 올려 saved object와 화면을 확인했는가

특히 런북은 운영 target restore에 `include_global_state:false`를 사용하도록 제한한다. source의 allocation filter, read-only block, recovery throttle 같은 설정이 새 환경에 그대로 따라오면 target cluster를 망칠 수 있기 때문이다.

### 2.7 약 17:29 이후 — 기준표를 실제 환경에 맞게 채우라는 부분

자동 전사에는 다음 표현이 있다.

> “일단은 이걸 기준으로 드리고”
>
> “여기 작업은 … 하시라고 하세요”
>
> “이걸 기준으로 쓰는 이유가 …”
>
> “어쨌든 … 다르다”

이 부분의 실무적 의미는 런북을 그대로 복사해 제출하라는 것이 아니라, 런북의 기준표·체크리스트를 현재 환경에 맞게 채우라는 것으로 해석하는 것이 좋다.

현재 Xen/Rocky 환경은 VMware/Oracle Linux 런북과 다르다.

- 하이퍼바이저: VMware가 아니라 Xen 계열
- OS: Oracle Linux 9.6이 아니라 실제 VM은 Rocky Linux 9.6으로 확인됨
- VM 관리: VMware 미러 방식이 아니라 Xen VM 복사·이동 방식으로 별도 확인 필요
- 서비스 버전·구성: 실제 VM의 설정과 런북 예시가 다를 수 있음
- 네트워크: target 임시 IP, hostname, L4/VIP 구조를 실제 사내 환경에 맞게 확정해야 함

따라서 계획서에는 “런북 기준”과 “현재 서버에서 확인한 사실”을 나란히 적어야 한다. 런북을 근거로 삼되, 환경이 다르면 실제 채증 결과를 우선해 수정해야 한다.

### 2.8 약 18:32 이후 — CMS 등을 확인한 뒤 특정 IP를 연결한다는 부분

자동 전사에는 다음과 같이 잡혔다.

> “여기 CMS랑 …”
>
> “이제 요거 요거 다 하고 나서”
>
> “특정 IP 하나를 또 이런 식으로 물려주면 된다는 점이에요”

이 부분은 런북의 target 격리와 L4 시험 투입 단계에 해당한다.

의미는 다음과 같다.

1. DB와 데이터 서비스가 정상이어야 한다.
2. master·engine·gateway·CMS·chat-ui를 단계별로 올린다.
3. source IP나 운영 VIP를 바라보지 않는지 확인한다.
4. target을 처음부터 전체 L4 pool에 넣지 않는다.
5. 특정 테스트 IP 또는 제한된 내부 요청만 target으로 보내 본다.
6. 오류율·응답시간·로그·데이터 쓰기를 관찰한다.
7. 문제가 없을 때만 투입 범위를 넓힌다.

여기서 “특정 IP를 물린다”는 정확히 어떤 장비의 어떤 IP인지는 현재 녹음만으로 확정할 수 없다. L4 VIP, 테스트 클라이언트 IP, 내부 검증용 경로 중 어느 것인지 실제 네트워크 담당자에게 확인해야 한다. 다만 **전체 사용자를 한 번에 target으로 보내지 않고 소규모 경로로 먼저 검증한다**는 원칙은 분명하다.

---

## 3. 서비스별로 “복사”와 “복원”을 구분하는 표

| 대상 | VM 미러에 들어오는 것 | target에서 실제로 할 일 | 녹음의 핵심 의미 |
|---|---|---|---|
| OS·실행파일·설정 | Rocky/Oracle Linux, Java, Python, JAR, 설정파일 등 | OS/라이브러리 호환성·권한·checksum 확인 | VM 복사는 출발점이며 기동 검증이 필요함 |
| Elasticsearch | data 디렉터리와 노드 내부 상태 | 미러 data는 hold 보관, 빈 새 cluster 생성, Snapshot/Restore, 정책 별도 적용 | “그냥 복사하지 말고 Snapshot/Restore” |
| Kibana | 설정과 `.kibana*` 같은 physical index | ES 복원 후 Kibana index·alias·saved object 확인 | ES가 정상이어야 Kibana도 정상임 |
| Solr | Solr 파일과 collection 관련 상태 | 새 ZK/Solr cloud 구성, configset 업로드, 모든 collection BACKUP/RESTORE, alias 재구성 | collection을 빠뜨리면 전체 이관 실패 |
| ZooKeeper | dataDir·transaction log·ephemeral session 등 | clean ensemble을 새로 구성하고 필요한 설정만 API로 복원 | 오래된 ensemble 내부 상태를 원시 복사하지 않음 |
| Redis | 정적 설정, cluster state, AOF/RDB | 기존 cluster 재결합 시도, 실패 시 빈 3 master/3 replica 구성, 시스템 배포로 cache 재생성 | cache는 재생성 가능하므로 별도 원본 이관의 우선순위가 낮음 |
| MariaDB | 미러 DB 전체와 schema/table | writer/Scheduler 정지, 무결성 점검, Scheduler 영향 데이터만 선택 재적재·교정 | 전체 DB를 새로 dump/restore하는 방식이 아님 |
| Scheduler | 애플리케이션 설정과 job 코드 | 처음에는 OFF, 리허설에서 승인된 job만 101 한 대에서 시험, 최종 승인 후 기동 | 3대 동시 실행 시 중복 job 위험 |
| CMS·master·engine·gateway·chat-ui | JAR·설정·로그 경로 | DB/Redis/ZK/Solr/ES target 주소 확인 후 순차 기동 | 애플리케이션이 어느 주소를 바라보는지가 핵심 |
| L4/VIP | 네트워크 설정·pool 정보 | 처음에는 target 미등록, 특정 테스트 경로·소량 투입 후 확대 | “서비스가 뜸”과 “운영 트래픽을 받아도 됨”은 다름 |

---

## 4. 녹음과 런북의 연결표

| 녹음에서 들리는 내용 | 연결되는 런북 | 쉬운 뜻 | 실무상 주의점 |
|---|---|---|---|
| Elasticsearch를 Snapshot하고 Restore | 5.5, 11장, 14.2·14.4 | ES가 이해하는 백업 형식으로 복원 | data 폴더를 새 cluster에 그대로 넣지 않음 |
| Solr collection을 이야기함 | 5.4, 10장, 14.4 | 모든 collection·configset·alias를 옮김 | 하나라도 누락되면 NO-GO |
| Redis는 데이터가 없어도 됨 | 9장, 14.4 | cache는 원본 서비스에서 다시 만들 수 있음 | 무조건 삭제하지 말고 기존 cluster 재결합을 먼저 확인 |
| 안 되면 Redis cluster를 새로 만듦 | 9.2·9.3 | 기존 상태가 실패하면 빈 cluster로 재구성 | cluster state만 지우고 AOF/RDB를 남기면 안 됨 |
| 시스템 배포를 다시 함 | 9.4, 13장 | 새 Redis에 cache를 업무 기능으로 재생성 | 성공 로그만 보지 말고 실제 key·TTL·조회 확인 |
| MariaDB를 꺼 둬야 함 | 8장, 14.1·14.3 | source/target에서 동시에 쓰지 않게 함 | Scheduler·CMS writer를 먼저 차단 |
| Elasticsearch·Kibana 절차 확인 | 11장, 12장 | ES 데이터·정책·Kibana를 순서대로 복원 | global state 자동 복원 금지, 정책 별도 검증 |
| 특정 IP를 연결 | 6.2, 12장, 15장 | 격리 target에 제한된 테스트 요청만 보냄 | 전체 L4 pool에 바로 넣지 않음 |
| 기준을 실제 환경에 맞춰 사용 | 3장, 4장, 부록 B | 런북 양식을 현재 서버 값으로 채움 | VMware/OL9.6 가정을 Xen/Rocky 값으로 바꿔야 함 |

---

## 5. 녹음에서 명확한 내용과 추정해야 하는 내용을 나누기

### 명확하게 이해할 수 있는 내용

- Elasticsearch는 Snapshot/Restore가 핵심이다.
- Solr collection은 이관·복원해야 한다.
- Redis는 cache이므로 기존 cluster가 안 되면 새로 구성할 수 있다.
- Redis를 새로 구성했다면 시스템 배포로 cache를 다시 만든다.
- MariaDB와 애플리케이션 writer는 데이터 정합성을 위해 멈춰야 한다.
- 마지막에는 특정 테스트 경로 또는 IP로 연결해 확인한다.

### 런북과 대조해 의미를 보완한 내용

- “그냥 파일을 뜬다”는 표현은 Solr의 파일 자체를 무조건 복사한다는 뜻으로 확정할 수 없다. 런북상 최종 방법은 collection BACKUP/RESTORE다.
- “나머지는 옮겨가면 된다”는 표현은 실행파일·설정·일반 파일을 checksum 확인 후 보존한다는 의미로 해석했다. 분산 데이터 서비스를 모두 파일 복사로 처리한다는 뜻은 아니다.
- “특정 IP를 물린다”는 IP의 종류는 녹음만으로 확정할 수 없다. L4 VIP인지 테스트 클라이언트 경로인지 확인이 필요하다.

### 현재 전사만으로 확정할 수 없는 내용

- 00:00~00:57 부근의 일반 대화
- 12:57~15:34 사이의 긴 저음량 구간에서 정확히 어떤 명령·서비스를 말했는지
- 자동 전사의 `A1, A2...`, `클로스터`, `세컷 펌프`, `멜리치감`처럼 문맥에 맞지 않는 단어
- CMS 뒤에 붙은 서비스명이 정확히 무엇인지
- 특정 IP가 가리키는 장비와 실제 연결 방식

이 부분들은 정리본에서 사실처럼 확정하지 않았다.

---

## 6. 이전에 만든 1~3번 문서를 다시 볼 때 바꿔야 할 핵심 관점

이번 녹음과 런북을 반영하면, 기존 1~3번 문서의 큰 그림은 다음처럼 정리되어야 한다.

### 1번 이론 문서에서 강조할 점

“VM 이관”과 “서비스 데이터 이관”은 다른 층의 작업이다.

- VM 복사: OS·프로그램·설정·디스크를 운반하는 작업
- 서비스 복원: 각 서비스가 자기 방식으로 데이터를 다시 읽게 만드는 작업
- 연결 변경: 애플리케이션이 source가 아니라 target의 DB·Redis·Solr·ES를 보게 하는 작업
- 운영 전환: L4/VIP가 target으로 요청을 보내게 하는 작업

이 네 가지를 한꺼번에 “복사”라고 부르면 계획이 헷갈린다.

### 2번 이관 계획에서 강조할 점

계획의 중심은 “전부 새로 설치할까?”가 아니라 다음 의사결정이다.

1. VM 자체는 복사·이동으로 target에 준비한다.
2. target은 운영망과 분리해 부팅한다.
3. 서비스별로 기존 상태 재결합 또는 새 구성·복원 중 하나를 선택한다.
4. 리허설에서 대체 경로까지 재현한다.
5. 최종 쓰기를 멈춘 뒤 B1 백업·선택 동기화를 한다.
6. 특정 테스트 경로를 거쳐 GO/NO-GO를 결정한다.
7. 실패하면 source를 보존한 채 롤백한다.

### 3번 실제 방법 문서에서 강조할 점

명령어를 나열하기 전에 각 명령의 목적을 적어야 한다.

- `VM 복사 명령`: VM을 운반하기 위한 것
- `hold 이동`: 원본을 삭제하지 않고 보관하기 위한 것
- `cluster bootstrap`: 새 분산 클러스터를 만드는 것
- `Snapshot/Restore`: ES 데이터를 안전한 논리 백업으로 복원하는 것
- `collection RESTORE`: Solr collection을 복원하는 것
- `system deploy`: Redis cache를 다시 만드는 것
- `L4 test`: 운영 전체 투입 전 제한된 요청만 보내는 것

특히 실제 Xen/Rocky 서버에 실행할 때는 이 VMware/Oracle Linux 런북의 명령을 그대로 붙여 넣으면 안 되고, Xen 관리 방식·Rocky Linux 경로·실제 hostname/IP·권한을 별도로 확인해야 한다.

---

## 7. 팀장님께 설명할 수 있는 최종 요약

다음처럼 설명하면 된다.

> 이번 작업은 단순히 VM 디스크를 복사하고 IP만 바꾸는 작업으로 끝내지 않습니다. VM 복사본을 먼저 운영망과 분리된 target으로 부팅한 뒤, 서비스별 데이터 성격에 따라 복원 방법을 나눕니다. Elasticsearch는 data 디렉터리를 직접 재사용하지 않고 Snapshot/Restore를 사용하고, Solr는 모든 collection과 configset을 BACKUP/RESTORE합니다. Redis는 cache이므로 기존 cluster 재결합을 우선 확인하되 실패하면 빈 3 master/3 replica cluster를 만든 뒤 시스템 배포로 cache를 재생성합니다. MariaDB는 Scheduler와 모든 writer를 중지하고, 전체 DB를 덮어쓰지 않고 런북에서 정한 Scheduler 영향 데이터만 선택적으로 재적재·교정합니다. 데이터 서비스 검증 후 애플리케이션을 순서대로 기동하고 Scheduler는 마지막에 한 대만 제한적으로 올립니다. 마지막으로 특정 테스트 IP 또는 격리된 L4 경로로 검증한 다음, 문제가 없을 때만 운영 트래픽을 target으로 전환하고, 문제가 생기면 source를 보존한 채 롤백합니다.

한마디로 말하면:

> **VM은 복사해서 운반하고, 데이터는 서비스별 안전한 방식으로 복원하고, 트래픽은 마지막에 조금씩 연결한다.**

---

## 8. 이번 자료의 범위와 다음 작업

이번에는 요청대로 런북 자체를 다시 작성하지 않고, 녹음에서 설명한 내용을 원문 전사와 런북 대조 해설로 분리했다. 런북의 명령은 실행하지 않았고, 실제 서버에도 변경을 가하지 않았다.

이 자료를 기준으로 다음 단계에서 1~3번 PDF를 다시 고칠 때는 다음을 반영해야 한다.

- 1번: VM·pool·SR·cluster·서비스 데이터의 관계를 더 명확하게 구분
- 2번: “VM 복사 → 격리 부팅 → 서비스별 복원 판단 → 리허설 → 최종 동기화 → 제한 연결 → 운영 전환”을 계획의 맨 앞부터 설명
- 3번: 명령어마다 목적·영향·확인 결과·실패 시 다음 경로를 붙이고, VMware/Oracle Linux 명령과 Xen/Rocky 실제 명령을 분리

