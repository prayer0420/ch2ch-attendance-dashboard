# 작업 진행 규칙

이 작업공간에서 사용자를 안내할 때 다음 규칙을 반드시 따른다.

1. 파일 변경, 서비스 재시작, 삭제, 재부팅, 보안 설정 등 영향이 있는 명령은 한 번에 하나씩 안내한다. 조회·상태 확인처럼 변경을 일으키지 않고 서로 독립적인 명령은 사용자가 빠르게 진행할 수 있도록 2~4개씩 묶어서 안내할 수 있다.
2. 복붙용 긴 명령어보다 사용자가 직접 타이핑하기 쉬운 명령어를 우선한다.
3. 명령어를 제시하기 전에 지금 무엇을 확인하거나 변경하는지 설명한다.
4. 명령어의 각 부분이 무엇을 의미하는지 설명한다.
5. 실행 후 정상 결과와 비정상 결과를 알려준다.
6. 영향이 있는 작업은 사용자의 실제 결과를 확인한 뒤 다음 단계로 넘어간다. 함께 안내한 조회 명령은 전체 결과를 한 번에 확인할 수 있다.
7. 오류가 발생하면 원인을 확인하기 전에 여러 설정을 동시에 바꾸지 않는다.
8. 삭제, 재설치, 재부팅, 보안 설정 변경 전에는 영향과 복구 방법을 먼저 설명한다.
9. 기존 VM, 네트워크, 파일은 사용자의 명시적 승인 없이 삭제하거나 수정하지 않는다.
10. 비밀번호를 임의로 만들거나 화면에 노출하지 않는다.
11. UAC, BIOS, 회사 보안정책 관련 작업은 진행을 멈추고 먼저 영향과 선택지를 설명한다.
12. 사용자가 직접 실행하고 배우는 것을 우선하며, 대신 조작하지 않는다.
13. WHPX, NAT, TCG, CPU 모델 등 전문 용어는 처음 나올 때 뜻과 사용하는 이유를 설명한다.
14. 서비스나 기능을 확인할 때 실제 주소, 포트, 연결 방향을 함께 설명한다.
15. 한 단계가 끝났다고 추정하지 말고, 확인 결과를 근거로 다음 단계를 안내한다.
16. 답변은 다음 순서를 기본으로 한다: 목적 설명 → 명령어 → 명령어 해설 → 정상 결과 → 비정상 결과와 다음 조치.
17. 전체 목표까지 필요한 작업을 먼저 대단계와 소단계 두 단계 깊이로 정리하고, 현재 진행 중인 대단계와 소단계를 매 답변에 표시한다.
18. 설명 구조는 대단계와 소단계까지만 사용하며, 소단계 아래에 추가 번호 체계를 만들지 않는다.
19. 현재 단계가 전체 목표에서 왜 필요한지 연결해서 설명한다.
20. 모든 진행 답변의 앞부분에 진행 현황을 표시한다: 현재 대단계 번호와 전체 대단계 수, 현재 대단계의 소단계 전체 수, 완료한 소단계, 진행 중인 소단계, 남은 소단계.
21. 현재 소단계가 끝나기 전에는 다음 소단계의 변경 명령을 안내하지 않는다. 다만 현재 소단계를 판정하는 데 필요한 독립적인 조회 명령은 묶어서 안내할 수 있다.
22. 전체 계획과 현재 진행 현황은 대단계와 소단계 두 단계 깊이로만 표현한다.

## 현재 작업 맥락

- Windows 11 Home 로컬 PC에서 QEMU로 Oracle Linux 10 VM을 구축한다.
- WHPX를 우선 사용하고, CPU 기능과 x86-64-v3 노출 여부를 확인한다.
- Oracle Linux VM은 4 vCPU, 8GB RAM, 50GB 디스크, NAT 네트워크를 목표로 한다.
- Windows localhost:2222에서 VM SSH 22번 포트로 접속하는 구성을 목표로 한다.
- 기존 VirtualBox VM, 파일, 네트워크는 보존한다.

## 인수인계 기준

- 이 프로젝트의 전체 배경, Xen 이관 실습, 서비스별 판단, QEMU 작업 상태는 `extras/server-migration/CODEX_MASTER_HANDOFF.md`를 기준으로 한다.
- QEMU Oracle Linux 10 작업의 상세 명령과 재개 지점은 `extras/qemu-oracle-linux/QEMU_ORACLE_LINUX_HANDOFF.md`를 기준으로 한다.
- 메리츠 Oracle Linux 10 로컬 실습 또는 고객사 개발계 작업은 `docs/meritz-ol10/README.md`를 읽고, `docs/current/CURRENT_STATE.md`와 해당 회차·현장 문서를 함께 사용한다.
- 고객사에서 오류 대응을 요청하면 `docs/meritz-ol10/field/INCIDENT_QUICK_INDEX.md`로 증상을 분류한 뒤, 변경 전에 대상 환경·hostname·서비스·직전 작업·오류 전문·상태·관련 로그를 확인한다.
- 새 대화에서는 현재 작업이 실제 `tc-xen02 → tc-xen03`, 연습 `xen02 → xen01`, 로컬 QEMU 중 어느 트랙인지 먼저 구분한다.
- 서로 다른 트랙의 호스트명, IP, 포트, OS, 명령을 한 작업에 섞지 않는다.
